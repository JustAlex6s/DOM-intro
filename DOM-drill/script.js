let parent = document.querySelector('ul');
let child = parent.childNodes;
console.log(child);
for (let elem of child){
    if(child.nodeType === 1 && elem.innerHTML == "Fast and Furious"){
        
    }
}

console.log(childNodes);
